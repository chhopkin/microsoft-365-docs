name: Shared-AutoLabelAssign (deprecated)

on:
    workflow_call:
        inputs:
            PayloadJson:
                required: true
                type: string
            ExcludedUserList:
                required: true
                type: string
            ExcludedBranchList:
                required: true
                type: string
            AutoAssignUsers:
                required: true
                type: string
            AutoAssignReviewers:
                required: true
                type: string
            AutoLabel:
                required: true
                type: string
        secrets:
            AccessToken:
                required: true
            PrivateKey:
                required: true
            ClientId:
                required: true

jobs:
    noop:
        runs-on: ubuntu-latest
        steps:
            - run: echo "This workflow is no longer in use. It has been moved to MicrosoftDocs/tcp-workflows."
