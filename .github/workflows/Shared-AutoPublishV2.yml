name: Shared-AutoPublishV2 (deprecated)

on:
    workflow_call:
        inputs:
            PayloadJson:
                required: true
                type: string
            EnableAutoPublish:
                required: true
                type: boolean
            EnableAutoMerge:
                required: false
                type: boolean
        secrets:
            AccessToken:
                required: true
            PrivateKey:
                required: true
            ClientId:
                required: true

jobs:
    noop:
        runs-on: ubuntu-latest
        steps:
            - run: echo "This workflow is no longer in use. It has been moved to MicrosoftDocs/tcp-workflows."
