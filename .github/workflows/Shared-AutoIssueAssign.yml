name: Shared-AutoIssueAssign (deprecated)

on:
    workflow_call:
        inputs:
            PayloadJson:
                required: true
                type: string
            ExcludedUserList:
                required: false
                type: string
        secrets:
            AccessToken:
                required: true
            PrivateKey:
                required: true
            ClientId:
                required: true

jobs:
    noop:
        runs-on: ubuntu-latest
        steps:
            - run: echo "This workflow is no longer in use. It has been moved to MicrosoftDocs/tcp-workflows."
