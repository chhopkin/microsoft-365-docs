name: Shared-TierManagement (deprecated)

on:
    workflow_call:
        inputs:
            PayloadJson:
                required: true
                type: string
            EnableWriteSignOff:
                required: true
                type: string
            EnableReadOnlySignoff:
                required: true
                type: string
        secrets:
            AccessToken:
                required: true

jobs:
    noop:
        runs-on: ubuntu-latest
        steps:
            - run: echo "This workflow is no longer in use. It has been moved to MicrosoftDocs/tcp-workflows."
